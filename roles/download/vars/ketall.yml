
github_project: corneliusweig/ketall
release_version: "{{ ketall_version | default('latest') }}"
release_file: ketall-{{ arch }}-linux.tar.gz
binary_in_archive: ketall-amd64-linux
binary_file: ketall
completion_command: completion {{  ansible_user_shell | basename }}
download_unarchive: true

download_mode: download_gh
arch_map: "{{ go_arch_map }}"